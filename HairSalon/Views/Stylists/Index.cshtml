@{
  Layout="_Layout";
}

<center>@if (Model.Count != 0)
{
<ul>
@foreach (var stylist in Model)
{
    <li><a href="/stylists/@stylist.GetId()">@stylist.GetName()</a>      <a href="/stylists/@stylist.GetId()/update">Edit</a> | <a href="/stylists/@stylist.GetId()/delete">Delete</a></li>
      }
    </ul>
  }
  <h4><a href="/stylists/new">Add a new stylist</a></h4>
  <h4><a href="/">Return home</a></h4>
</div></center>
